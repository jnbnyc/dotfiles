# shortcuts + functions for vagrant
#   alias v='vagrant'
#   alias vup='vagrant up'
#   alias vs='vagrant status'
#   alias vdown='vagrant destroy --force' # Don't ask for confirmation before destroying
#   alias vp='vagrant provision'
#   alias vin='vagrant up && vagrant ssh'
#   vbounce() { vagrant destroy $1 --force; vagrant up $1; }
